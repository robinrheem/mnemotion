[project]
name = "mnemotion"
version = "0.1.0"
description = "Memory-Persistent Long-Form Video Synthesis."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "typer>=0.24.1",
]

[dependency-groups]
dev = [
    "pre-commit>=4.5.1",
]
